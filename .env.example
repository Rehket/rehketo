# rehketo-ui dev env — copy to .env.local if you need to override.
#
# Empty default means "same-origin" — the Vite proxy (dev) or the API's
# StaticFiles mount (prod) handles routing. Only override if you're
# pointing the UI at a backend on a different origin (NOT recommended —
# cookies break).
PUBLIC_API_BASE=
