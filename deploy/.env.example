# Used by docker-compose. Copy to .env and fill secrets.
POSTGRES_USER=rehketo
POSTGRES_PASSWORD=rehketo

# Required for Plan 2 ship-check (live Claude Sonnet via Bifrost).
# Leave blank if you only plan to run the test suite (tests mock Bifrost).
ANTHROPIC_API_KEY=
